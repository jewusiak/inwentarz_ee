name: inwentarz_ee
description: Aplikacja inwentaryzacyjna - Grzegorz Jewusiak /EE.

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  shared_preferences:
  flutter_lints:
  google_fonts:
  intl:
  flutter_secure_storage:
  file_picker:
  permission_handler:
  provider:
  sn_progress_dialog:
  carousel_slider:
  url_launcher:
  photo_view:
  mobile_scanner:
  printing:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/

